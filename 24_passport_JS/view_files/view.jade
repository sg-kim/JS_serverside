doctype html
html
	head
		meta(charset='utf-8')
	body
		h1
			a(href='/topic') Server Side Javascript
		ul
			each topic in topics
				li
					-id= topic['id']
					a(href='/topic/' + id)= topic['title']
		article
			if topic
				h2= topic['title']
				=topic['description']
				div= 'by ' + topic['author']
			else
				h2 Welcome
				| Server side Javascript tutorial w/ MySQL
		ul
			li
				a(href='/topic/add') add
			if topic
				-id=topic['id']
				li
					a(href='/topic/' + id + '/edit') edit
				li
					a(href='/topic/' + id + '/delete') delete

